import {blogMock} from '../../molecules/BlogCard/Blog.mock';
const feedMock = {
    blogs: [
        blogMock,
        blogMock,
        blogMock
    ],
    category: 'Marketing'
}

export {
    feedMock
}